<%=link_to "ダム一覧へ", dams_path %>
<h4><%= @user.name %>さんのお気に入り一覧</h4>


<% @favorites.each do |favorite| %>
	<div>
		<%= link_to dam_path(favorite.dam.id) do %>
			 <% favorite.dam.title %>
		<% end %>
	</div>
<% end %>